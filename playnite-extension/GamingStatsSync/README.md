# Gaming Stats Sync for Playnite

Uploads a bounded, non-sensitive Playnite library snapshot, local artwork, and
now-playing presence to a configured Gaming Stats API deployment.

The extension never uploads executable paths, install directories, ROM paths,
launch arguments, scripts, notes, cookies, or store credentials. It requires a
dedicated Playnite upload key and only accepts an HTTPS API URL.
